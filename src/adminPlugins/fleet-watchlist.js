/** Generated by the ADMIN plugin builder. */
export default {
  id: 'fleet-watchlist',
  label: 'Fleet Watchlist',
  description: 'Saved vessels, refreshed on demand.',
  render(container) {
    const heading = document.createElement('h3');
    heading.id = 'fleet-watchlist-heading';
    heading.textContent = 'FLEET WATCHLIST';
    const body = document.createElement('p');
    body.id = 'fleet-watchlist-body';
    body.textContent = 'Tracking 3 vessels.';
    container.append(heading, body);
    return () => { window.__fleetWatchlistTornDown = (window.__fleetWatchlistTornDown || 0) + 1; };
  },
};
